<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agriassitance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ass"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ass"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Gov"></activity>
        <activity android:name=".dises" />
        <activity android:name=".Medi" />
        <activity android:name=".Scro" />
        <activity android:name=".one" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>